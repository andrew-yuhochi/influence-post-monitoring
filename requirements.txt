# Influence Post Monitoring — pinned dependencies
# Python 3.11+

# Twitter ingestion (cookie-based scraping)
twikit==2.3.3

# Market data
yfinance==0.2.54
finvizfinance==1.3.0
pandas_market_calendars==5.3.2

# NLP / Ticker extraction
spacy==3.8.13

# LLM scoring
anthropic==0.95.0

# Data validation & config
pydantic==2.13.1
pydantic-settings==2.13.1

# WhatsApp delivery (primary)
twilio==9.10.5

# HTTP (CallMeBot fallback + async)
httpx==0.28.1

# Database (Turso cloud SQLite — via direct httpx HTTP calls to /v2/pipeline)

# Env file loading (for __main__ entrypoints)
python-dotenv==1.2.2

# Scheduling (local dev fallback)
APScheduler==3.11.2

# Testing
pytest==9.0.3
pytest-cov==7.1.0
pytest-asyncio==1.3.0
